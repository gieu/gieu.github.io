---
// import { Image } from "astro:assets";
const { src, alt, width = 25, loading = "eager", ...props } = Astro.props;
---

<!-- Why did i this this ?  -->
<img src={src} alt={alt} width={width} loading={loading} decoding="async" />
